server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/Garber-Shooter/tensquare_dev.git
  rabbitmq:
    host: 192.168.206.129
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh